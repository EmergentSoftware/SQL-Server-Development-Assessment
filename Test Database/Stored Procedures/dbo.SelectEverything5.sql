SET QUOTED_IDENTIFIER ON
GO
SET ANSI_NULLS ON
GO
CREATE PROCEDURE [dbo].[SelectEverything5]
AS
BEGIN
    SET NOCOUNT ON;

    IF EXISTS (SELECT * FROM dbo.NewspaperReader) BEGIN
		SELECT 1;
    END;

END;
GO
